import { requirePermission } from "@/server/auth/session";import { prisma } from "@/server/db/client";export const dynamic="force-dynamic";export default async function AuditPage(){await requirePermission("audit:view");const events=await prisma.auditEvent.findMany({orderBy:{occurredAt:"desc"},take:100});return <main className="page"><div className="page-heading"><div><p className="eyebrow">Accountability</p><h1>Audit trail</h1><p>Append-only history of sensitive workflow events.</p></div></div><section className="table-card"><table><thead><tr><th>Time</th><th>Action</th><th>Entity</th><th>Actor</th><th>Request</th></tr></thead><tbody>{events.map(e=><tr key={e.id}><td>{e.occurredAt.toLocaleString("en-PH")}</td><td><strong>{e.action.replaceAll("_"," ")}</strong></td><td>{e.entityType}<small>{e.entityId.slice(0,8)}</small></td><td>{e.actorId.slice(0,12)}</td><td>{e.requestId.slice(0,12)}</td></tr>)}</tbody></table></section></main>}
